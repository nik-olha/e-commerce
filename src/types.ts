export type SliderItem = {
    id: number
    img: string
    title: string
    desc: string
    bd: string
}

export type SliderItems = SliderItem[]

export type Category = {
    id: number
    img: string
    title: string
}

export type Categories = Category[]

export type PopularProduct = {
    id: number
    img: string
}

export type PopularProducts = PopularProduct[]
